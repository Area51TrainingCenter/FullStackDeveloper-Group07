import { AppSelectDirective } from './app-select.directive';

describe('AppSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSelectDirective();
    expect(directive).toBeTruthy();
  });
});
